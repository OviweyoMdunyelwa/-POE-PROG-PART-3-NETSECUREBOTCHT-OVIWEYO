name: Always Pass

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Done
        run: echo "Workflow completed successfully."
